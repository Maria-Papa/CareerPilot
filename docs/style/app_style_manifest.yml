APP_STYLE_MANIFEST:
  name: "CareerPilot - Code, Formatting & Writing Rules"

  git_commit_style:
    convention: "Conventional Commits"
    format: "<type>(<scope>): short imperative summary"

    subject_rules:
      tense: "present"
      max_length: 72
      punctuation: "no period at end"
      casing: "lowercase"

    allowed_types:
      feat: "New feature or capability"
      fix: "Bug fix"
      refactor: "Code change without behavior change"
      docs: "Documentation only"
      test: "Adding or updating tests"
      chore: "Tooling, config, maintenance, or developer experience"

    recommended_scopes:
      architecture:
        - backend
        - frontend
        - api
        - infra
        - style
        - tooling
        - ci
        - adr

      domain:
        - jobs
        - interviews
        - files
        - auth
        - users
        - companies

      persistence:
        - models
        - enums
        - db
        - migrations

    scope_rules:
      - "Scopes must be lowercase"
      - "Use singular nouns"
      - "Multiple scopes allowed: feat(models, enums)"
      - "Avoid generic scopes unless necessary"
      - "ADR commits should reference ADR number in scope, e.g., docs(adr-005): clarify repository responsibilities"

    body_rules:
      bullet_style: "-"
      tone: "clear, concise, professional"
      intent: "Explain why the change exists, not implementation details"

END_STYLE_MANIFEST
